<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="umn.ac.id.uas_mobile_musicplayer">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UAS_Mobile_MusicPlayer">
        <activity android:name=".Halaman_DaftarLagu" />
        <activity android:name=".Halaman_Profil" />
        <activity android:name=".Halaman_Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="umn.ac.id.uas_mobile_musicplayer.MainActivity" />
        </activity>
        <activity android:name=".Popup"
            android:theme="@style/Theme.AppCompat.CustomTheme"/>
    </application>

</manifest>